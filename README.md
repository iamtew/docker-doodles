Various Dockerfile's for different purposes. Mostly small doodles I use for
different test cases, and/or just random stuff.

Usage:
```bash
$ docker run --interactive --tty --rm iamtew/doodles:fedora /bin/bash
```
